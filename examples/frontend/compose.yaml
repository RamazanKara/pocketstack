services:
  app:
    image: node:22-alpine
    working_dir: /workspace
    command: npm run dev -- --host 0.0.0.0
    environment:
      VITE_POCKETSTACK_MODE: browser
    ports:
      - "5173:5173"
    volumes:
      - .:/workspace
